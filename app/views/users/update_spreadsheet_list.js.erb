$('#paginate-loading-layer').remove();

<% if defined? error %>
  alert("<%= t('.spreadsheet_not_imported') %> <%= error %>");
<% else %>
  $('#no-spreadsheet').remove();
  $('.list').empty();
  $("<%= escape_javascript(render 'spreadsheets/spreadsheet') %>")
    .appendTo(".spreadsheet");
<% end%>
