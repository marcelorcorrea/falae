<!DOCTYPE html>
<html>
  <head>
    <title>Fala&ecirc;</title>
    <%= favicon_link_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta property="og:title" content="Fala&ecirc;" />
    <meta property="og:description" content="<%= t '.og_description' %>" />
    <meta property="og:image" content="<%= image_url 'og_image_falae_widescreen.png' %>" />
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
  </head>

  <body>
    <div class="container">
      <header>
        <%= render 'layouts/header' %>
      </header>
      <div class="content">
        <%= render 'layouts/nav_menu' if show_nav_menu? %>
        <div class="view">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
