$('.page-items').empty();
<% locals = { columns: @page.columns, items: @page.items, rows: @page.rows } %>
$("<%= escape_javascript(render 'pages/items', locals) %>")
  .appendTo(".page-items");
$('.page-items').trigger('change');
